# last_verified: 2026-08-22 · Ansible n/a
# Minimal idempotent playbook to install nginx and ensure it's running.
# Run with: ansible-playbook install-nginx.yml

- name: install and start nginx
  hosts: webservers
  become: true
  tasks:
    - name: install nginx package
      ansible.builtin.apt:
        name: nginx
        state: present
        update_cache: true
      when: ansible_os_family == "Debian"

    - name: ensure nginx service is running and enabled
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: true
