# Copy to .env and adjust. COMPOSE_PROFILES enables the "tools"
# profile (migrate, db-shell); leave it empty for the default stack.
POSTGRES_USER=app
POSTGRES_DB=appdb
API_PORT=8000
WEB_PORT=8080
COMPOSE_PROFILES=tools
